# Consul

It's a service networking platform, it provides

- Service Discovery: central registry for services in the network, allows for direct communication
- Service Mesh: manages network traffic between services
- Application Configuration capabilities
